# T14.0 — Chisel Evidence Inventory & Corroboration Audit

Status: CLOSED
Mode: AUDIT
Risk: R3
Branch: `staging`
Depends on: Sprint 13 CLOSED. Production behavior verified at `ca67561`. Closed repo state `fbe9389`.
Unlocks: the evidence that can shape the next Sprint 14 question. Not an automatic `T14.1`.
Inherits: `planning/sprints/sprint-14-iwl-decomposition-corroboration.md` (this activation); public Chisel Article/IWL @ `fbe9389`; `PLANNING.md` §13 @ `fbe9389`

## Goal

Compare the published Chisel IWL with the authorized development artifacts. Determine which claims those artifacts support, where the authorial account goes beyond them, whether the four layers overlap, and which cases pressure future grammar.

## Why now

Sprint 13 closed the outer envelope and published the Chisel pair. Sprint 14's question cannot be answered from that envelope. The public IWL already names drafts, reviews, and reasons. Those claims are now the fixture Sprint 15 would otherwise treat as structure.

## Starting state

Verified before content inspection, by filename and hash only:

- Public baseline @ `fbe9389`: `site/src/content/iwl/the-chisel-fallacy.md` and `site/src/content/research/the-chisel-fallacy-what-pangram-actually-measures/index.md`. The IWL has the four named layers plus Document Identity, Version History, Methodology Note, and Tools and Infrastructure. The Article includes `IWL Significant Events — Draft v6`.
- Publication fixtures, hashes match the T13.2 record: `C:\Users\amuli\Downloads\ChiselFallacy_v6.docx` (`e282841c6898d4e319e217e551cc165b02ecec669b7a98ea8fd0190f103bc3fc`) and `ChiselFallacy_IWL_v2.docx` (`8171321b4fc2f81dfe2c89405112b9ec1943aa6d6473d3e4fac33e2ca0b72bc7`). Authored baseline, not proof of earlier history.
- Earlier versions present: `ChiselFallacy_v5.docx`, `ChiselFallacy_v3.pdf`, `ChiselFallacy_IWL.docx` (unversioned filename; self-identified version is an audit fact, not a second source).
- Absent from Downloads, Desktop, and Documents under the authorized names: paper v1, v2, and v4; position 3 reviews of v1, v3, v4, and v6; separate source-check notes; separate naming notes.
- `ChiselFallacy_v7.pdf` exists and is not authorized. Do not open it.
- Private output directory `C:\chisel-sprint14-private\` is not inside a Git worktree. `C:\` has no repository. Home toplevel is `C:/Users/amuli`. Site toplevel is `C:/Users/amuli/Desktop/cybernetics-mbp`.

## Decisions already frozen

- Amul authorized the artifact categories above and accepted the R3 evidence exception on 2026-09-23.
- Published documents and hash-matched publication fixtures are the authored baseline.
- Discovery Context is an authorial account. Observable draft changes stay distinct from claims about why they occurred.
- Statement kind and evidence outcome stay audit labels. Sprint 13's outer contract stays frozen, including semantic silence and opaque IWL body.

## Questions this ticket may answer

- Which meaningful public records have an authorized anchor, and which are gaps?
- For the deep sample, what did the source actually change, and what remains retrospective?
- Do units recur across the four layers, and which units resist a shared-object reading?
- What should the next ticket ask, if anything?

## Scope

- Inventory authorized sources, then the public IWL's meaningful records. The IWL cannot corroborate itself.
- Map every meaningful public record to an anchor or to an explicit gap.
- Deeply check a varied subset: VMA emergence, one rejected alternative, one factual correction, one reviewer-input claim, one authorial-motivation claim, and one dependency/proof-chain claim. Record why each was sampled.
- Keep statement kind separate from evidence outcome.
- Test the four layers as editorial views. Preserve messy relationship wording.
- Write four private outputs and a bounded public summary.

## Explicit exclusions

- Raw chats, model transcripts, hidden reasoning, `ChiselFallacy_v7.pdf`, and any undesignated substitute.
- Public IWL or Article edits. Schema, enums, validators, graph UI, CMS, runtime, routes, indexing.
- Drafting `T14.1`. Harmonizing a contradiction into a cleaner story.

## Implementation contract

- Source of truth for authorization is this ticket. Source of truth for the public words is the repo at `fbe9389`. Source of truth for a draft change is the designated file that contains it.
- Absence of v1, v2, v4, reviews, or notes is a gap. Do not reconstruct the missing text from the IWL's description of it.
- The four private files are the only durable private outputs: `A-evidence-inventory.md`, `B-corroboration-matrix.md`, `C-decomposition-observations.md`, `D-pressure-log.md`.
- Commit only a non-reconstructive summary. No quotation long enough to rebuild a private draft.

## Acceptance criteria

1. The inventory records what was authorized, what was opened, and what is missing, with the privacy boundary intact.
2. The matrix accounts for every meaningful public IWL record, with location and an explicit gap where the anchor is missing. The varied subset has source-level results and stated limits.
3. Statement kind and evidence outcome stay separate. A draft change is not recorded as the author's reason. A review mention is not recorded as an inspected review.
4. The four-layer hypothesis is tested on repeated material and on resistant cases. Omissions, compression, and chronology-versus-cause stay unresolved where the evidence is insufficient.
5. The four outputs are only in `C:\chisel-sprint14-private\`. The public diff stays inside `planning/` and `ROADMAP.md`. The conclusion names the earned next question without drafting it.

## Validation matrix

| Layer | Check | Expected result |
|---|---|---|
| Source boundary | SHA-256 of the two publication fixtures; filename inventory | Hashes match T13.2; missing categories listed as gaps |
| Output boundary | `git rev-parse` from `C:\chisel-sprint14-private` | Fatal: not a git repository |
| Claim boundary | Deep-sample rows name the file and what it does not show | No motive inferred from a diff; no missing draft reconstructed |
| Public diff | `git diff --check` and name-only diff | Only `planning/` and `ROADMAP.md` |

## Adversarial tests

- Record one Discovery Context block as an authorial account even where a related draft change is visible. Expected: motive stays unsupported by the diff.
- Do not cite v6 or IWL v2 as independent proof of an event the version table places before those files. Expected: those rows say baseline, not corroboration.
- Leave v1's claimed contents unreconstructed. Expected: VMA-absence-from-v1 stays unavailable.
- Leave every position 3 claim without a review file as unavailable. Expected: no adoption inferred.
- Restoration: read-only. Proof is an empty diff outside `planning/` and `ROADMAP.md`.

## Regression boundaries

- Public IWL and Article bytes unchanged. Sprint 13's closed contract unchanged.
- No schema, validator, route, index, or workflow change.
- `ChiselFallacy_v7.pdf` unopened.

## Documentation updates

- Activation writes this ticket, the Sprint 14 contract, the program-arc present-tense reconciliation, and the `ROADMAP.md` pointer.
- `ROADMAP.md` also corrects the false statement that `ai-systems` has no published paper, and the same stale corpus count beside it.
- `CHANGELOG.md` is not edited here. Sprint closure owns that promotion.
- The completion commit may update this ticket, the sprint status line, the program-arc gate, and the roadmap pointer. It may not add other surfaces.

## Git and deployment boundary

- Commit and push `staging` only. No merge to `main`. No production deploy.
- R3 choreography: this seal commit, then one completion commit. No runtime implementation commit.
- R3 evidence exception, accepted by Amul 2026-09-23: the risk is private-evidence handling, not routing or deploy. Required proof is the diff boundary plus the four private outputs. `npm run build`, staging verification, and production re-verification are not required.
- Stop if the diff includes `src/`, `public/`, `scripts/`, or a workflow file.

## Stop conditions

Stop if access would exceed the designated set, a required file or the output boundary is ambiguous, private evidence would enter Git or the public report, raw chats become necessary, or a material public IWL claim looks false enough to need a correction decision.

## Required completion report

Use `planning/templates/completion-report.md`. R3 cap: 100 lines. Point to the four private files. Do not paste private draft text.

## Done when

The authorized evidence is inventoried, the comparison and the unresolved cases are recorded with honest coverage, the public diff is confined to the planning and roadmap files, and the next question can be shaped without an ontology and without a pre-written `T14.1`.

# T14.0 Completion Report

Result: COMPLETE WITH JUSTIFIED DEVIATION
Branch: staging
Commit: seal `5b04719`; closure hash is `git log -1 --format=%H -- site/planning/tickets/T14.0-iwl-evidence-map-audit.md`
Deployment: none. Accepted R3 exception: no build, no staging check, no production check.

## Outcome

The authorized set was inventoried and compared. v3, v5, and v6 show several wording changes. v1, v2, v4, and every position 3 review are gaps. Discovery Context stays an authorial account, and the VMA note itself differs between the two opened IWL files. The four layers overlap on a few episodes and fail as a shared-object model for Tools and for Layer 4A. No schema and no public IWL edit.

Private outputs, outside every Git worktree: `C:\chisel-sprint14-private\A-evidence-inventory.md`, `B-corroboration-matrix.md`, `C-decomposition-observations.md`, `D-pressure-log.md`.

## Files changed

| File | Change | Why |
|---|---|---|
| `planning/sprints/sprint-14-iwl-decomposition-corroboration.md` | Activated, then marked EXECUTING | Sprint contract |
| `planning/tickets/T14.0-iwl-evidence-map-audit.md` | Sealed, then this report | Authorization and result |
| `planning/programs/research-object-arc-13-17.md` | Present-tense Sprint 13 gate and Sprint 14 exit | Stale program direction |
| `ROADMAP.md` | Sprint 14 pointer; corpus-count corrections | Current-work index |

## Acceptance criteria

| Criterion | Result | Evidence |
|---|---|---|
| 1 Inventory and gaps | Pass | Output A. Hashes match T13.2. Missing v1/v2/v4, reviews, and notes named |
| 2 Matrix and deep sample | Pass | Output B. Six sample rows have limits |
| 3 Kind separated from outcome | Pass | Wording changes marked direct; motives and reviews marked authorial or unavailable |
| 4 Four-layer test, tensions kept | Pass | Output C and D. Domain-count and Karpathy rows unresolved |
| 5 Locations and diff boundary | Pass | Four files only in the private directory. Public diff is `planning/` and `ROADMAP.md` |

## Validation performed

| Check | Result | Key output |
|---|---|---|
| Publication-fixture SHA-256 | Pass | Both match the T13.2 hashes |
| Private directory Git boundary | Pass | `git rev-parse` fatal; not inside `C:/Users/amuli` or the site repo |
| `git diff --check` and path list | Pass on both commits | No `src/`, `public/`, `scripts/`, or workflow path |
| `npm run build` / staging | Waived | Amul accepted the R3 exception. Risk is private evidence, not deploy |

## Adversarial tests

| Mutation | Expected | Observed | Restored |
|---|---|---|---|
| Discovery Context as motive | Not direct evidence | Authorial only | Read-only |
| v6 / IWL v2 as proof of pre-publication events | Refused | Marked baseline | Read-only |
| Reconstruct v1 | Refused | VMA-absence stays unavailable | Read-only |
| Infer review adoption | Refused | Position 3 rows unavailable | Read-only |

## Deviations from ticket

- Ticket named the ai-systems count and the corpus count beside it. The same ROADMAP edit also corrected two neighboring sentences that still said Pangram was unpublished or not yet existing (the relations bullet and the Goodhart tag bullet). Same false corpus fact. No other surface added.
- `ChiselFallacy_IWL.docx` was opened as an earlier IWL. Its own identity line says artifact version v4, not IWL v1. It was not relabeled and not treated as a substitute for a missing v1 file.

## Newly discovered findings

- `ChiselFallacy_v7.pdf` is on disk and was not opened.
- The Article's section 10 tests need v1 and v2, which are not in the set.
- No public correction is earned. The Karpathy and domain-count tensions are unresolved, not a demonstrated false sentence with an authorized replacement.

## Regression confirmation

Public IWL and Article were not edited. Sprint 13's closed contract was not edited. Indexing, schema, and workflows were not edited.

## Documentation reconciliation

Program arc and ROADMAP now say `T14.0` is closed and no next ticket is sealed. `CHANGELOG.md` was left for sprint closure so this diff stayed inside `planning/` and `ROADMAP.md`.

## Remaining gate

Sprint 14 stays EXECUTING. The earned question, not drafted: if paper v1, v2, or v4 exists outside the searched folders, authorize it before any grammar work. Without that, Sprint 15 would treat version-history claims this audit could not check. Planner review is the next gate. No `T14.1`.
