# T13.4 — Production Promotion + Sprint 13 Closure

Status: CLOSED
Mode: RELEASE
Risk: R3
Branch: `staging` → `main`
Depends on: T13.3 CLOSED at `318109f`; process calibration at `c0e345b`
Unlocks: Sprint 14 planning after Sprint 13 production verification and closure
Inherits: `planning/sprints/sprint-13-iwl-foundation.md` @ `318109f`; `planning/tickets/T13.3-iwl-outer-contract-validation.md` @ `318109f`; `PLANNING.md` §13 @ `c0e345b`

## Goal

Promote the T13.3-validated Sprint 13 runtime to production without changing it, verify the production layer, then close Sprint 13. This is a release gate; T13.3 owns the deep validation.

## Pre-seal disposition — resolved before seal

1. **Breadcrumb AC2** — literal criterion (`<nav aria-label>`) confirmed unmet; the shared `Breadcrumbs.astro` renders `<p class="breadcrumbs meta">`, sitewide, pre-existing, not IWL-specific, not a regression. Presented to Amul directly as an explicit accept-or-block choice (not silently waived). **Decision: accepted** — known gap, proceed with promotion; no correction ticket routed. Recorded here, not in T13.3, which stays unrewritten.
2. **Pagefind** — T13.3's own local-rebuild inference was insufficient for a release gate. Resolved with real, direct, deployed-staging evidence instead: a headless-Chrome session drove the actual `/search` page (`PagefindUI`) against `staging.cybernetics-mbp-site.pages.dev`, queried `"Discovery Context Authorial Rationale"` (text unique to the IWL body), and got 8 real results — the IWL page itself plus four of its real internal-section anchors (`#layer-1--development-ledger`, `#layer-2--decision-record`, `#layer-3--authorial-accountability`, and two Article-side anchors referencing the IWL) — confirming the deployed search index genuinely contains the IWL's authored content, not merely a local-build inference.

Both dispositions resolved; proceeding to seal.

## Authorized delta

- Record the exact `staging` release-candidate SHA and pre-promotion `main` SHA; confirm candidate ancestry from `318109f`, no runtime/content changes since T13.3, and green candidate CI. **Recorded**: candidate = `318109f` (current `staging` HEAD, = T13.3's own closure commit); pre-promotion `main` = `ce93ade` (unchanged since Sprint 12); `git merge-base --is-ancestor origin/main origin/staging` confirms a clean fast-forward path; the 11 commits between them are exactly the Sprint 13 sequence, no stray commits; candidate CI (run for `318109f`) already confirmed green at T13.3's own closure.
- Seal this ticket on `staging`; confirm its commit changes planning text only and rerun the required CI gate.
- With Amul's production acceptance, fast-forward `main` to the exact sealed staging commit; record the promoted SHA. Do not cherry-pick or force-update.
- Verify production directly, then make one closure/documentation commit on `staging` and fast-forward that docs-only closure to `main`. Confirm its final CI/deployment succeeds.
- Reconcile current Sprint 13, `ROADMAP.md`, `CHANGELOG.md`, and relevant current canonical instructions. Fix stale present-tense fixture/status language without rewriting closed tickets or historical changelog entries. Reference the three unrelated T13.3 findings once; leave their fixes for separately earned work.

## Exclusions

No runtime or content edits; no IWL architecture, inner grammar, PDF, JSON-LD, or indexing-policy change; no fixes for the three unrelated T13.3 findings or the accepted breadcrumb gap; no Sprint 14 ticket or implementation. Production defects stop this release instead of becoming an in-ticket patch.

## Acceptance checks

1. The promoted runtime/content tree matches T13.3's validated state at `318109f`; the seal and closure introduce documentation changes only. `main` advances by traceable fast-forward promotions, with no unrelated branch commits.
2. Production deployment/workflow is green for the first promotion. The Chisel Article and subordinate IWL routes return `200` at their canonical paths; the Article's scholarly PDF remains available.
3. The Article links to its IWL; the IWL's "Companion to" link resolves to its Article. Canonical URLs and sitewide CC BY-NC 4.0 license remain correct.
4. The IWL emits no JSON-LD; its PDF path remains `404`. Both new pages retain `noindex, nofollow`; robots/indexing policy is unchanged.
5. The production Pagefind index contains the actual IWL URL and nonempty authored content, verified the same real end-to-end way as the pre-seal disposition (live search UI), not local-build inference. The sitemap includes the Article and IWL under existing behavior.
6. One or two existing research sentinels retain their expected canonical, DOI, `audience`, and relation semantics where applicable, plus their existing PDF availability. Cite T13.3 for the full four-entry and six-viewport matrices; do not repeat them.
7. The AC2 disposition and deployed-staging Pagefind result are recorded honestly; no unresolved release blocker is labeled PASS.
8. Only after production checks pass: T13.4 and Sprint 13 are closed in current documentation, Sprint 14 becomes eligible, closure docs are promoted to `main`, and their final deployment/CI is green. Inner IWL grammar remains open.

## Stop conditions and rollback

Stop before promotion if the ancestry, candidate diff, pre-seal dispositions, approval, or CI gate fails. Stop after promotion if production differs from the validated contract, a route/PDF/search boundary fails, or an unrelated commit enters the release lineage. Record the last known good `main` SHA and use the repository's existing deployment/rollback procedure; do not force-push or repair production inside this RELEASE ticket.

## Validation and completion report

Use the current repo's established release/CI checks plus the production assertions above. Cite exact commit SHAs, workflow/deployment URLs, route results, and the two pre-seal dispositions; keep the report within the R3 100-line cap. No synthetic unpublished probe, citation matrix, or viewport sweep is repeated.

## Done when

The exact validated outer Article/IWL contract is live and independently verified on production, closure documentation is on `main` with final CI green, Sprint 13 is CLOSED, and Sprint 14 may be planned.

---

# T13.4 Completion Report

Result: COMPLETE
Branch: staging → main
Commit: seal `ca67561`; Promotion 1 fast-forward `ce93ade..ca67561`; closure commit resolved via `git log -1 --format=%H -- planning/tickets/T13.4-production-promotion-sprint-13-closure.md`
Deployment: production (`amulbham.com`), verified directly

## Outcome

The validated Sprint 13 outer envelope is live on production, independently verified — not assumed from workflow success.

## Acceptance criteria

| # | Result | Evidence |
|---|---|---|
| 1 | PASS | `ce93ade` is an ancestor of `318109f`; range contains only the Sprint 13 sequence; `ca67561` (seal, docs-only) pushed as clean fast-forward to `main` |
| 2 | PASS | Production: Article `200`, IWL `200`, Article `paper.pdf` `200` |
| 3 | PASS | Article→IWL link and IWL "Companion to" link both resolve correctly on production; both pages show sitewide CC BY-NC 4.0 |
| 4 | PASS | Zero JSON-LD on production IWL page; IWL `paper.pdf` → `404`; both pages `noindex, nofollow` |
| 5 | PASS | Real production `/search` (PagefindUI) query for IWL-unique text returns the IWL page + 3 real section anchors; sitemap includes both URLs |
| 6 | PASS | Invariants sentinel on production: `200`/PDF `200`, DOI `identifier` and `audience` verbatim-unchanged; Three SOS `isBasedOn` verbatim-unchanged |
| 7 | PASS | Both pre-seal dispositions recorded honestly in the seal record above — no blocker mislabeled |
| 8 | PASS | This closure commit + Promotion 2 close it out below |

## Deviations from ticket

None. The two pre-seal dispositions were resolved exactly as the ticket required before seal, not deferred.

## Regression confirmation

Invariants (canonical/DOI/audience/PDF) and Three SOS (`isBasedOn`) confirmed byte-identical on production against known T12.4/T13.3 values.

## Documentation reconciliation

`planning/sprints/sprint-13-iwl-foundation.md` → `CLOSED`, exit state marked achieved with `main @ ca67561`. `ROADMAP.md` Sprint 13 entry replaced with a completion pointer + Sprint 14 pointer. `CHANGELOG.md` gains this closure entry. The three T13.3 findings and the accepted breadcrumb gap are referenced once each, not re-litigated; none fixed here.

## Remaining gate

Promote this closure documentation commit to `main` (Promotion 2) and confirm its deploy is green — no further product verification needed, since no runtime diff exists beyond what Promotion 1 already verified. Sprint 14 becomes eligible once that lands.
