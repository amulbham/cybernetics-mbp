# T18.0 — Publication Artifact & Rendering Audit

Status: CLOSED
Mode: AUDIT
Risk: R2 (public artifacts and source inspection; documentation-only tracked changes)
Branch: `staging`
Depends on: Sprint 15 CLOSED, `main @ 8e60f6b`; Sprint 18 activation
Unlocks: evidence-based choice of the next smallest Sprint 18 operation, not automatic authorization
Inherits: `planning/sprints/sprint-18-scholarly-publication-pipeline.md` (activation); `PLANNING.md` v1.1, `PUBLISHING.md`, `AGENTS.md`, `scripts/build-research-pdfs.mjs`, `scripts/validate-research-pdfs.mjs`, `src/styles/print-research.css` @ `8e60f6b` (paths relative to `site/`)

## Goal

Audit the actual published corpus and pipeline. Identify the smallest safe PDF identity migration and separate repeated rendering/validation needs from isolated editorial defects. Do not implement changes.

## Why now

Sprint 15 closed on OPAQUE V1. Publication-pipeline mechanics are the first directional pre-launch need; existing successful validators do not prove artifact appearance or table usability.

## Starting state

Published paper PDFs use `paper.pdf`; the live corpus is recorded as four papers plus an essay and one published IWL at the baseline. Reconfirm the current published set before counting artifacts or tables. The roadmap leaves Sprint 18 unauthorized until activation.

## Decisions already frozen

Product authority requires `{existing canonical paper slug}.pdf` in the article HTML directory, derived from the same route/content identity used by `canonicalPath()`. This is an artifact identity requirement, not a Google Scholar filename requirement. Preserve one authoritative PDF URL. The compatibility mechanism remains open.

## Questions this ticket may answer

- Which owners/consumers and deployed routes must change for the filename migration? What happens to existing `paper.pdf` links, and what is the least disruptive redirect, alias, or other supported treatment?
- Which PDF defects recur across published papers, and which are paper-specific? Which checks are automatable stable invariants, manual artifact QA, or not earned?
- Which actual table shapes need different screen or print behavior? Can behavior be inferred reliably, or would later author intent be required?

## Scope

1. Map the PDF identity chain: builder output, slug/`canonicalPath()` source, `paperPdfUrl`, Highwire `citation_pdf_url`, JSON-LD `MediaObject.contentUrl`, download link and filename behavior, validator, documentation, and deployed/static routing. Compare old-path compatibility options without choosing an implementation in advance.
2. Inspect **every published paper PDF** (currently four): route/name, size, searchable text, page count, first-page identity hierarchy, references, fonts where inspectable, table/callout behavior, pagination, endings, and repeated versus isolated defects. Use raster/visual inspection for layout judgments. Report **Scholar/publication integrity** separately from **print/artifact quality**.
3. Inventory **every published long-form page that contains a table**, including papers, essay, and IWL as applicable. Inspect representative desktop/mobile screen behavior; inspect PDF table behavior only for papers with PDFs. Record table shape, wrapping/overflow, focusability when no overflow exists, header behavior, PDF fragmentation, and whether reusable handling can be inferred or would need authored intent. Working classes are observations, not schema or required design.
4. Reconfirm only the four named housekeeping findings (ArticleMasthead ORCID display, Header `<h2>` before page `<h1>`, FormattedDate UTC/display behavior, unused starter images) and directly relevant publication findings already recorded in `ROADMAP.md` or T13.3. Do not hunt for more. Incidental discoveries are `NEW FINDING`, outside this ticket's cleanup scope.
5. Write one bounded public evidence report at `planning/audits/T18.0-publication-artifact-rendering.md`: A. PDF pipeline/identity map; B. published-paper artifact matrix; C. live-table matrix; D. repeated versus isolated defects; E. smallest possible next-operation recommendation. Recommendations are not authorization.

## Explicit exclusions

No PDF rename, redirect/alias, print CSS, table markup/CSS/rehype, validator, Article/IWL content, Highwire/Schema.org, indexing/robots/sitemap, Home/About, Production Ready scoring, IWL PDF, Sprint 16/17, or T18.1+ drafting. No changes to runtime, public assets, or deployment configuration.

## Implementation contract

Treat the desired PDF filename as frozen, but derive its exact per-paper slug from existing canonical identity. Assess old direct links and single-URL authority against actual static/deployment behavior; do not assume a 301 is available. Do not equate parsed/searchable PDFs with visually correct pages. Label proposed checks `AUTOMATABLE STABLE INVARIANT`, `MANUAL ARTIFACT QA`, or `NOT EARNED`; implement none. Keep the report bounded to evidence and explicit unknowns.

## Acceptance criteria

1. All published paper PDFs are represented, with Scholar/integrity and visual/print findings separated; artifact count is based on the current corpus, not a hard-coded four.
2. All published long-form pages with tables are covered for screen rendering; PDF table observations are limited to actual paper PDFs. Non-table pages are explicitly excluded from that matrix.
3. The canonical-slug source, every PDF owner/consumer, old-link behavior, and at least the viable compatibility choices are mapped; a future ticket can implement one authoritative PDF identity without inventing URL rules.
4. Repeated defects, isolated defects, manual QA, stable validators, and not-earned checks are distinguished with representative evidence.
5. The housekeeping appendix is limited to the recorded set. One next-operation recommendation is made without drafting or authorizing it. Tracked changes are documentation-only.

## Validation matrix

| Layer | Check | Expected result |
|---|---|---|
| Corpus/identity | Enumerate published entries, PDF outputs, consumers and old/new routes | No paper or owner omitted; slug source and compatibility limits explicit |
| Artifact | PDF metadata/text checks plus rendered-page inspection for every paper | Integrity and visual conclusions independently evidenced |
| Tables | Inventory published Markdown tables; inspect representative screen widths and applicable PDF pages | Every table-bearing page covered; overflow/print observations tied to examples |
| Boundary | `git diff --check`, changed-file list, audit report against A–E | Only `planning/` and a necessary `ROADMAP.md` pointer; no runtime/content change |

Existing build/validator output may be used as evidence for stable properties, but cannot substitute for visual inspection. R2 evidence exception: this ticket changes no product behavior, so artifact inspection, adversarial analysis, corpus coverage, and the diff boundary replace implementation mutations/full validator-chain repetition. No staging/production promotion is required; inspect deployed routes only where they answer the PDF identity question.

## Adversarial tests

- Check a non-overflowing table against the current focusable scroll wrapper; record whether focus conveys useful behavior.
- Check a wide table on mobile and in PDF; do not assume a single behavior fits both.
- Challenge a proposed PDF validator with a visually broken yet parseable page; classify the visual judgment as manual.
- Check a direct old `paper.pdf` URL and a candidate new slug URL as migration cases; do not create either route.

## Regression boundaries

All published content, current routes, PDFs, metadata, IWL no-PDF rule, search/index policy, and deployment behavior remain unchanged by T18.0.

## Documentation updates

Activate this sprint and ticket, add a short terminal handoff in `planning/programs/research-object-arc-13-17.md`, and replace the directional pipeline row in `ROADMAP.md` with an active Sprint 18 pointer. Keep the later phases directional and unnumbered. Correct a directly related stale publication count only after verifying the live corpus; avoid general cleanup. Append the bounded evidence report and completion reconciliation at closure.

## Git and deployment boundary

Verify the `8e60f6b` baseline and clean `staging` ancestry. After `BLOCKER / REQUIRED / OPTIONAL` pre-seal review, seal this ticket and sprint activation on `staging` before auditing. Commit the report/completion after evidence is gathered and push `staging`; no `main` promotion or manual deployment. An automatic workflow is not proof of audit conclusions. Do not edit any closed sprint/ticket.

## Stop conditions

Stop for Amul if migration requires changing Article canonical identity, old-link compatibility poses a material product tradeoff, tables require unapproved author-facing semantics, Scholar guidance conflicts materially with the publishing model, a PDF projection would change content, or the audit exposes a major architectural dependency outside Sprint 18.

## Required completion report

Append the standard R2 report from `planning/templates/completion-report.md` (50-line cap). Link the A–E evidence report; state the pre-seal disposition, seal/closure SHAs, corpus coverage, key findings, exact changed paths, and one next recommended operation. Do not draft it.

## Done when

The full relevant published corpus is inspected, migration dependencies and compatibility options are explicit, repeated pipeline issues are separated from isolated issues, and the next smallest operation is recommended without runtime changes.

# T18.0 Completion Report

Result: COMPLETE
Branch: staging
Commit: seal `f07f67c`; closure hash is `git log -1 --format=%H -- site/planning/tickets/T18.0-publication-artifact-rendering-audit.md`
Deployment: none. No `main` promotion. Staging push is not audit evidence.

## Outcome

Pre-seal: no BLOCKER. Evidence is `planning/audits/T18.0-publication-artifact-rendering.md`. Four paper PDFs, one essay with no tables and no PDF, one IWL with 14 tables and no PDF.

## Files changed

This ticket; the Sprint 18 contract; the program-arc handoff and gate; `ROADMAP.md`; the new audit report.

## Acceptance criteria

1 Pass. Integrity and print quality are separate. Count follows the live corpus. 2 Pass. Table pages are the four papers plus the IWL. The essay is excluded. 3 Pass. Slug source is `canonicalPath()` / `entry.id`. Old `paper.pdf` is 200; the slug URL is 404; no redirect exists. 4 Pass. ORCID split is repeated; the IWL-link title lead-in is one paper. 5 Pass. Housekeeping stayed the recorded four. One next operation is named and not drafted.

## Validation performed

Production `paper.pdf` files downloaded and parsed. Essay and IWL `paper.pdf` and one candidate slug URL returned 404. A 390px capture showed the article header wrapping. Table overflow is the shipped CSS, not a page-by-page raster. `git diff` is documentation only.

## Adversarial tests

Non-overflow tables still get `tabindex="0"`. Wide tables and PDF tables do not share a proven layout. A parseable PDF is not treated as visually correct. No new route was created.

## Deviations from ticket

None material. PDF table pages were not rastered one by one; that limit is stated in the evidence report.

## Newly discovered findings

`.iwl-link` is not print-hidden and precedes the Chisel title. Recorded as `NEW FINDING`, not added cleanup.

## Regression confirmation

No runtime, content, route, PDF, validator, or indexing file changed.

## Documentation reconciliation

Sprint 18 stays EXECUTING. Later corpus and discovery phases stay unnumbered. `CHANGELOG.md` waits for sprint closure.

## Remaining gate

Product authority chooses the old `paper.pdf` treatment. Until then, the recommended next operation is not authorized: one ticket that moves the literal, the builder, and the validator together to `{entry.id}.pdf`.
