# T12.4 — Production Promotion + Sprint 12 Closure Freeze

**Status:** CLOSED
**Mode:** RELEASE
**Risk:** R3
**Branch:** `staging → main`
**Release-candidate base:** `8c6a230`
**Production baseline:** `main @ ad85aa0`
**Depends on:** `T12.3` closed and accepted
**Unlocks:** Sprint 13 / `T13.0` — IWL Foundation audit

> **Seal instruction.** Commit this populated `READY` ticket to `staging` before release work begins. After the seal commit, the contract below is immutable. T12.4 may perform the explicitly authorized documentation reconciliation below, promote the already-validated release candidate, verify production, and close Sprint 12. It may not introduce new Reader Context behavior.

---

## Goal

Promote the fully validated Sprint 12 Reader Context system from `staging` to production, independently verify the production deployment, perform the final closure freeze, and close Sprint 12.

T12.4 also absorbs two directly adjacent documentation/governance corrections discovered **before sealing**:

```text
1. CHANGELOG.md
   → remove one duplicate T12.2 heading

2. PLANNING.md
   → add proportionality / scope-economy rules
   → clarify planner handling of DEFECT
   → add proportional-governance maxim to §13
```

These are intentionally incorporated into the T12.4 contract before seal because they are low-risk, directly adjacent to the release/closure purpose, and create one clear unified contract rather than independent work.

No additional cleanup is implied.

---

## Why now

Sprint 12 has completed its implementation and validation work:

```text
T12.0
→ corpus / behavior audit

T12.1
→ readerNote source model
→ FromTheAuthor human surface

T12.1.1
→ real mobile regression verification
→ visual polish

T12.2
→ readerNote.for → Article.audience
→ readerNote.why remains human-only
→ independent semantic validator

T12.3
→ full end-to-end contract QA
→ zero runtime defects found
```

T12.3 verified source truth, human rendering, accessibility, responsive containment, TOC separation, semantic projection, validator independence, PDF exclusion, format neutrality, true absence, and Sprint 8–11 regressions.

No new Reader Context capability remains to implement.

The remaining work is release and closure.

---

## Starting state

Verified before drafting this ticket:

```text
staging
→ 8c6a230
→ T12.3 closure

main
→ ad85aa0
→ Sprint 11.7 production baseline
```

T12.3's two-commit choreography is complete:

```text
531c527
→ T12.3 seal
→ CI/deployment green

8c6a230
→ T12.3 closure
→ CI/deployment green
```

T12.3 found:

```text
runtime defects       0
semantic defects      0
responsive defects    0
accessibility defects 0
PDF defects           0
```

One clerical closure artifact was subsequently found in `CHANGELOG.md`:

```text
## 2026-09-14 — T12.2: Reader Context audience projection + independent validator migration

## 2026-09-14 — T12.2: Reader Context audience projection + independent validator migration
```

Only the heading is duplicated. The T12.2 body appears once.

The Planning System v1 calibration also revealed a governance issue worth freezing before the next program arc: procedural rigor needs an explicit proportionality constraint so that low-risk adjacent work does not automatically manufacture additional tickets or reproduce higher-risk evidence unnecessarily.

---

# Decisions already frozen

T12.4 must not reopen any Sprint 12 product decision.

Frozen Reader Context contract:

```text
readerNote
├── why
└── for
```

* Module optional.
* Inner object strict and complete-or-absent.
* Eligibility is editorial presence/absence.
* No runtime inference from format, voice, pillar, tags, DOI, or content analysis.
* Invariants and Three SOS carry Reader Context.
* FAFSA and Frontier intentionally do not.
* `FromTheAuthor` is the sole human Reader Context surface.
* Reader Context remains outside TOC/heading state.
* Reader Context remains PDF-hidden.
* `readerNote.for → Article.audience`.
* `readerNote.why` remains human-only.
* `Article.backstory` remains absent.
* Semantic validation remains independent of the production serializer.
* Indexing remains disabled.

T12.4 is not authorized to change any of these.

---

# Scope

## 1. Seal T12.4

Create:

```text
planning/tickets/T12.4-production-promotion-sprint-12-closure.md
```

as `READY`.

Commit it to `staging`.

Push and require the staging workflow to complete successfully before continuing.

Record the seal commit externally after it exists.

Do not embed a future self-referential commit hash into the ticket.

---

# 2. Pre-promotion documentation reconciliation

Before promoting `staging → main`, make exactly the bounded corrections below.

## 2A. Repair the duplicate CHANGELOG heading

In `CHANGELOG.md`, remove exactly one of the two immediately adjacent duplicate T12.2 headings.

Required result:

```text
T12.3 entry
↓
one T12.2 heading
↓
existing T12.2 body
```

Do not modify:

```text
T12.2 body
T12.3 body
historical evidence
dates
commit references
validation findings
```

Run a narrow scan around the current Sprint 12 entries for another immediately duplicated ticket heading.

Do not turn this into a general changelog rewrite.

---

## 2B. Add proportionality and scope economy to `PLANNING.md` §7

Add a subsection under **Change-control rules**.

Use the following contract:

### Proportionality and scope economy

Planning rigor must be proportional to the risk and independence of the work.

The purpose of tickets, gates, and corrective records is to preserve correctness, authority, and traceability — not to maximize procedural granularity.

Use the **smallest governance unit that safely contains the work**.

### Before seal

Before a ticket is sealed, directly adjacent low-risk work may be incorporated when doing so preserves a clear, unified contract.

Such incorporation must not hide a materially different product decision, architecture change, source-of-truth change, privacy boundary, or risk class inside an otherwise unrelated ticket.

### After seal

After sealing, a finding may be absorbed only when it:

* falls within the ticket's already-authorized surfaces;
* introduces no new product, semantic, architectural, editorial, privacy, or source-of-truth decision;
* introduces no materially different risk;
* can be validated by the ticket's existing acceptance criteria without changing the contract.

Otherwise:

```text
defer it
or
create a separate ticket
```

Do not rewrite the sealed contract to make newly discovered work appear pre-authorized.

Examples of work that may reasonably remain inside an appropriate existing gate include:

* removing a duplicate Markdown heading during an already-authorized documentation closure;
* correcting a clerical typo in an active document already being reconciled;
* making a narrow implementation-detail correction already required by an existing acceptance criterion.

Create a separate ticket when the finding introduces an independent unit of risk, authority, decision, or verification — for example:

* a runtime or product defect outside authorized implementation;
* a new semantic or editorial decision;
* a route, identity, schema, privacy, indexing, or source-of-truth change;
* a substantive correction to the historical record of a closed ticket;
* work that can fail independently and should not contaminate the current ticket's evidence;
* work belonging to another sprint's core question.

Do not create a ticket solely because a defect exists or because the planning hierarchy has somewhere to put one.

Likewise, do not absorb work merely to avoid creating a ticket.

The governing test is:

> **Does separating this work materially improve safety, authority, traceability, or evidence?**

If not, prefer the simpler path.

Evidence depth follows the risk classes in §5. Apply only the minimum evidence required by the actual risk.

Reference established validators and procedures when they already provide the required proof; do not reproduce higher-risk validation merely because an earlier ticket used it.

---

## 2C. Clarify Planner Review in `PLANNING.md` §11

Add one narrow rule beneath the existing planner-review classifications:

> A `DEFECT` classification does not automatically imply a new ticket. The reviewer first applies the proportionality rule in §7 and determines whether the defect can be safely absorbed by an already-authorized adjacent gate. A new ticket is required when separation materially improves safety, authority, traceability, or evidence, or when the existing sealed contract does not authorize the work.

Do not otherwise redesign Planner Review.

---

## 2D. Extend the canonical Planning Maxim in §13

Append a second maxim to the existing canonical **Planning maxim** section:

> **Apply the minimum governance necessary for the actual risk. More process is justified only when it materially improves safety, authority, traceability, or evidence.**

Do not create a competing maxim elsewhere in the document.

The existing planning maxim remains intact.

---

## 2E. Planning-system boundaries

Do not modify:

```text
planning/templates/
planning/programs/
ticket lifecycle states
risk-class definitions
work-type definitions
source-of-truth ownership
```

This is a calibration of how existing governance is applied, not Planning System v2.

---

# 3. Verify the reconciled release candidate on staging

After the documentation reconciliation:

```bash
git diff --check
npm run build
npm run build:pdfs
npm run validate:pdfs
npx astro check
```

Required:

```text
build
→ PASS

build:pdfs
→ PASS

validate:pdfs
→ PASS

astro check
→ clean OR exactly the known pre-existing baseline
→ no new diagnostics
```

Evidence depth follows the existing R3 requirements in `PLANNING.md` §5.

Do not reproduce T12.3's full adversarial QA matrix merely because it exists.

The established validators and T12.3 evidence already prove the pre-release product contract; this step confirms that the bounded documentation reconciliation did not disturb it.

Require the staging Actions workflow to finish successfully.

Verify the documentation diff contains only the authorized pre-release reconciliation plus T12.4 ticket state.

No Reader Context runtime file should change.

---

# 4. Pre-promotion branch gate

Before merging to `main`, confirm:

```text
main still represents the expected production baseline
```

Expected at ticket drafting:

```text
main = ad85aa0
```

If `main` has changed independently, stop and inspect the change before promotion.

Do not blindly merge onto an unexpected production baseline.

Confirm the release candidate contains all Sprint 12 work and no unresolved runtime mutation.

Confirm the working tree is clean.

---

# 5. Promote staging → main

Promote the verified staging state through the repository's existing release mechanism:

```text
staging
→ main
```

Use the normal merge/PR process already documented in `AGENTS.md`.

Do not alter product files during the merge.

Do not flip indexing controls.

The production promotion must trigger the existing GitHub Actions pipeline.

Use the repository's current workflow as authority for its exact steps rather than copying an assumed pipeline order into this ticket.

Workflow success alone does not prove production correctness.

---

# 6. Production verification

After the production workflow is green, independently verify `amulbham.com`.

Use cache-busting where appropriate.

T12.4 does **not** rerun T12.3's entire seven-viewport adversarial matrix.

T12.3 already established the contract.

This release verification is a focused production smoke test.

---

## Human Reader Context

Verify on production:

```text
Invariants
→ FromTheAuthor present

Three SOS
→ FromTheAuthor present

FAFSA
→ absent

Frontier
→ absent
```

Check source-faithful visible `why` / `for` text on at least one Reader Context page.

---

## Semantic Reader Context

Directly inspect production Article JSON-LD.

Require:

| Object     | `audience`       | `backstory` |
| ---------- | ----------------- | ----------- |
| Invariants | exact `Audience` | absent      |
| Three SOS  | exact `Audience` | absent      |
| FAFSA      | absent            | absent      |
| Frontier   | absent            | absent      |

Present `audience` must remain:

```json
{
  "@type": "Audience",
  "audienceType": "<authored readerNote.for>"
}
```

No additional fields.

---

## Reading-shell smoke test

On one Reader Context paper:

```text
mobile width
→ contained
→ mobile TOC behavior intact

desktop width
→ contained
→ desktop TOC behavior intact
```

A phone-width and desktop-width production check is sufficient here.

Do not repeat T12.3's full responsive matrix unless the smoke test exposes a discrepancy.

---

## PDF production verification

For all three paper routes:

```text
paper.pdf
→ HTTP 200
```

Spot-check one Reader Context paper artifact and confirm Reader Context remains excluded.

The permanent PDF validator's green production workflow remains additional evidence.

---

## Existing semantic/regression smoke checks

Confirm production still preserves:

```text
canonical URL identity
author/publisher canonical Person
Invariants DOI
Three SOS isBasedOn → Invariants
```

No need to rerun Sprint 11's adversarial mutations.

---

## Indexing state

Confirm production remains deliberately deindexed:

```text
robots.txt
→ Disallow: /

research pages
→ noindex, nofollow
```

Do not enable indexing.

---

# 7. Production failure handling

If production differs materially from the verified staging release candidate:

```text
STOP
```

Do not close Sprint 12.

Determine whether the problem is:

```text
deployment/cache issue
runtime regression
artifact failure
branch/promotion error
```

Use the existing rollback procedure documented in `AGENTS.md` when production rollback is warranted.

Do not invent a new release mechanism inside T12.4.

A runtime/product defect requiring code changes is not repaired inside this release ticket unless the already-sealed contract explicitly authorized that surface and correction.

Otherwise, defer it or create a separately bounded corrective ticket under the §7 proportionality rule.

---

# 8. Closure freeze

Only after production verification passes, perform the final Sprint 12 documentation reconciliation.

Update:

```text
planning/tickets/T12.4-production-promotion-sprint-12-closure.md

planning/sprints/sprint-12-reader-context.md

ROADMAP.md

CHANGELOG.md
```

---

## Sprint contract

Change:

```text
Sprint 12
→ CLOSED
```

Record all tickets:

```text
T12.0    CLOSED
T12.1    CLOSED
T12.1.1  CLOSED
T12.2    CLOSED
T12.3    CLOSED
T12.4    CLOSED
```

Record:

```text
production verified
closure freeze complete
Sprint 13 unlocked
```

---

## ROADMAP

Remove Reader Context as active/in-progress work.

Record Sprint 12 as complete and production-verified.

Set the next research-object program step to:

```text
Sprint 13
→ IWL Foundation

Next earned operation
→ T13.0 architecture/reality audit
```

Do not pre-create Sprint 13 implementation tickets merely for numbering symmetry.

---

## CHANGELOG

Append one T12.4 entry describing:

```text
production promotion
production verification
Sprint 12 closure
```

Also record the two bounded pre-release reconciliation items:

```text
duplicate T12.2 heading removed

Planning System v1 calibrated with proportionality/scope-economy rule
```

Do not describe either as a separate ticket.

Do not rewrite earlier historical entries.

---

# 9. Final documentation-drift scan

Before Sprint 12 is declared closed, scan active current-state documentation for stale claims including:

```text
Next: 12.3
Next: 12.4
Sprint 12 in progress
staging-only Reader Context
Reader Context not in production
no Reader Context
audience forbidden
publisher not decided
production pending
```

Inspect at minimum:

```text
ROADMAP.md
PLANNING.md
planning/sprints/sprint-12-reader-context.md
PUBLISHING.md
AGENTS.md
active planning Markdown
relevant current code comments
```

Historical CHANGELOG statements remain historical.

Do not manufacture edits when files are already correct.

The closure freeze updates only genuinely stale present-tense instructions.

If a newly discovered issue would require changing an already-sealed unauthorized surface, apply §7's post-seal rule rather than silently broadening T12.4.

---

# 10. Publish the closure record

The closure documentation must eventually reach `main`; Sprint 12 is not fully closed if production code is current but canonical repository documentation still says the sprint is executing.

Recommended sequence:

```text
A. T12.4 seal on staging
B. pre-release documentation reconciliation on staging
C. promote verified release candidate to main
D. verify production
E. closure-freeze documentation on staging
F. promote closure documentation to main
G. require final docs-only production workflow green
```

The final docs-only promotion does not require repeating the full production Reader Context verification.

It does require:

```text
workflow green
no product/runtime diff since verified production release
production remains reachable
```

After the final promotion, `main` and `staging` must be content-equivalent with respect to the closed Sprint 12 state.

They need not have identical SHA topology if the merge mechanism creates merge commits.

---

# Explicit exclusions

T12.4 may not introduce:

```text
new Reader Context fields
new Reader Context eligibility
new component behavior
new CSS
new responsive behavior
new Schema.org property
Article.backstory
new relation projection
IWL implementation
new content
memo fixture
indexing enablement
route change
canonical identity change
PDF architecture change
deployment architecture change
planning template redesign
risk-class redesign
ticket-state redesign
```

Do not touch runtime/product code unless required to abort or roll back a failed release.

A production defect requiring an unapproved code fix is not repaired inside this release ticket.

---

# Proportionality boundary

This ticket intentionally incorporates the duplicate changelog-heading repair and Planning System calibration **before seal** because both are directly adjacent low-risk work and preserve a coherent release/closure contract.

After T12.4 is sealed, any additional finding must satisfy the newly documented post-seal rule:

```text
already-authorized surface
+
no new decision
+
no materially different risk
+
existing acceptance criteria can validate it
```

If not:

```text
defer
or
separate ticket
```

Do not confuse streamlined governance with unbounded scope.

---

# Acceptance criteria

T12.4 passes only when:

1. T12.4 is prospectively sealed before release work.
2. The duplicate T12.2 CHANGELOG heading is reduced to exactly one heading without altering the historical body.
3. `PLANNING.md` §7 contains the pre-seal/post-seal proportionality rule.
4. `PLANNING.md` §7 references §5 for evidence depth rather than defining a competing evidence ladder.
5. `PLANNING.md` §11 states that `DEFECT` does not automatically imply a new ticket.
6. `PLANNING.md` §13 contains the proportional-governance maxim.
7. No template or broader planning-system redesign occurs.
8. Pre-promotion staging build/PDF validation is green.
9. `astro check` introduces no new diagnostic.
10. Production baseline is confirmed before merge.
11. Verified staging state is promoted to `main`.
12. Production Actions deployment succeeds.
13. Production Reader Context human matrix is correct.
14. Production `audience`/`backstory` semantic matrix is correct.
15. Production phone + desktop smoke tests show no reading-shell regression.
16. All three production paper PDFs return successfully and Reader Context remains excluded.
17. Critical canonical identity / DOI / relation semantics remain intact.
18. Production remains `noindex, nofollow` with `robots.txt: Disallow: /`.
19. No production-only regression is found.
20. Sprint 12 contract is marked `CLOSED` only after production verification.
21. ROADMAP no longer presents Sprint 12 as active.
22. T12.4 completion is appended to CHANGELOG without rewriting history.
23. Final active-document drift scan finds no stale Sprint 12 state.
24. Closure documentation is promoted to `main`.
25. Final docs-only production workflow is green.
26. No runtime/product change occurs after the verified production release.
27. Sprint 13 / T13.0 is the sole next research-object program gate.

---

# Validation matrix

| Layer               | Check                                   | Required result                  |
| -------------------- | ---------------------------------------- | ---------------------------------- |
| Seal                | T12.4 ticket committed before execution | PASS                             |
| Changelog repair    | Duplicate T12.2 heading                 | Exactly one remains              |
| Planning §7         | Pre-seal/post-seal proportionality      | Present                          |
| Planning §5 linkage | Evidence-depth rule                     | References existing risk classes |
| Planner review      | `DEFECT` clarification                  | Present                          |
| Planning §13        | Proportional-governance maxim           | Present                          |
| Staging build       | `npm run build`                         | PASS                             |
| PDFs                | build + validator                       | PASS                             |
| Astro               | `npx astro check`                       | No new diagnostics               |
| Production baseline | `main` inspected before merge           | Expected / reconciled            |
| Promotion           | staging → main                          | Successful                       |
| Production CI       | Existing release workflow               | PASS                             |
| Human surface       | 4-object matrix                         | Exact                            |
| Semantics           | audience/backstory matrix               | Exact                            |
| Responsive smoke    | phone + desktop                         | No regression                    |
| PDF production      | 3 paper artifacts                       | 200 / valid                      |
| Regression smoke    | canonical/Person/DOI/isBasedOn          | Unchanged                        |
| Indexing            | noindex + robots                        | Unchanged                        |
| Closure docs        | sprint/roadmap/changelog                | Current                          |
| Drift scan          | active docs                             | No stale Sprint 12 claim         |
| Final promotion     | closure docs → main                     | Successful                       |
| Final workflow      | docs-only production run                | PASS                             |
| Runtime diff        | after verified release                  | None                             |
| Next gate           | program state                           | T13.0 only                       |

---

# Git / release choreography

Use proportional release choreography.

## Commit A — seal

On `staging`:

```text
T12.4 ticket only
```

Push.

Require CI green.

Record the resolved seal commit after it exists.

---

## Commit B — pre-release reconciliation

On `staging`:

```text
CHANGELOG.md
→ duplicate heading repair

PLANNING.md
→ §7 proportionality/scope-economy rule
→ §11 planner-review clarification
→ §13 proportional-governance maxim
```

No runtime changes.

Push.

Require CI green.

---

## Promotion 1 — production release

Promote verified `staging` to `main`.

Require production workflow green.

Independently verify production.

If verification fails:

```text
Sprint 12 stays open
T12.4 stays open
```

---

## Commit C — closure freeze

After production verification, on `staging`:

```text
T12.4
→ CLOSED + completion report

Sprint 12 contract
→ CLOSED

ROADMAP
→ Sprint 12 complete
→ Sprint 13 / T13.0 next

CHANGELOG
→ T12.4 release + Sprint 12 closure entry

any genuinely stale active documentation
→ reconciled
```

No runtime changes.

The completion report committed in Commit C records evidence available **through production verification**.

It does not attempt to contain Commit C's own final hash or evidence that can exist only after Commit C is committed.

Push and require staging CI green.

---

## Promotion 2 — closure documentation

Promote the closure-freeze documentation to `main`.

Require final production workflow green.

Verify:

```text
no runtime diff since Promotion 1
production reachable
main documentation reflects Sprint 12 CLOSED
```

### Self-referential closure evidence

After Promotion 2, the external executor handoff must report:

```text
Commit C resolved hash

final promotion reference

final workflow result
```

These are external repository/workflow evidence.

Do **not** create another commit merely to backfill those self-referential values into the T12.4 completion report.

Do not repeat the entire T12.3 QA matrix.

---

# Stop conditions

Stop before Sprint 12 closure if:

* `main` changed unexpectedly before promotion;
* staging build/PDF validation regresses;
* the pre-release reconciliation touches runtime behavior;
* production deployment fails;
* production differs materially from staging;
* Reader Context presence/absence differs in production;
* audience/backstory semantics differ;
* paper PDFs fail;
* indexing changes;
* canonical identity/DOI/relation semantics regress;
* a runtime fix becomes necessary;
* final documentation reconciliation uncovers a substantive unresolved product contradiction;
* post-seal newly discovered work falls outside T12.4's already-authorized surfaces or introduces new risk/decision.

Do not solve a failed R3 release by widening this ticket into implementation work.

---

# Required completion report

Use the canonical completion-report structure, but keep the report proportional.

The completion report written in Commit C records evidence through successful **Promotion 1 production verification**.

Record:

* Result
* Branches
* Seal commit
* Pre-release reconciliation commit
* Production promotion reference
* Production workflow
* Production verification summary
* Files changed through Commit C
* Acceptance criteria that can be established through production verification
* Documentation corrections
* Planning-system calibration
* Regression confirmation
* Deviations
* New findings
* Remaining closure steps

Do not embed Commit C's own future hash, Promotion 2 reference, or final workflow result inside Commit C.

After Promotion 2, the external executor handoff reports:

* Commit C's resolved hash;
* final closure-documentation promotion reference;
* final production workflow result;
* confirmation that no runtime/product diff occurred after Promotion 1;
* final Sprint 12 closure state.

No additional commit is created merely to backfill those values.

---

# Done when

Sprint 12 has moved through the complete lifecycle:

```text
AUDIT
→ IMPLEMENT
→ POLISH
→ SEMANTIC PROJECTION
→ FULL CONTRACT QA
→ PRODUCTION RELEASE
→ CLOSURE FREEZE
```

and the repository truth is:

```text
Reader Context
→ production

readerNote source model
→ frozen

FromTheAuthor
→ production

readerNote.for → Article.audience
→ production

readerNote.why → no machine projection
→ frozen

PDF exclusion
→ production

Sprint 12
→ CLOSED

Planning System v1
→ calibrated toward proportional governance

Sprint 13
→ UNLOCKED

Next
→ T13.0 — IWL Foundation architecture/reality audit
```

The planning system now preserves both of its governing principles:

> **Audit reality → implement the smallest earned grammar → adversarially test it → freeze only what survives.**

and:

> **Apply the minimum governance necessary for the actual risk. More process is justified only when it materially improves safety, authority, traceability, or evidence.**

---

# T12.4 Completion Report

Result: COMPLETE through Promotion 1 production verification. (This report is written in Commit C and, per the ticket's own instruction, does not embed Commit C's own hash or any Promotion 2 evidence — see "Remaining closure steps" below for what the external executor handoff must still report.)

Branches: `staging` (seal `7a64963`, pre-release reconciliation `b64e381`) → `main` (Promotion 1, fast-forward `ad85aa0` → `b64e381`)

Seal commit: `7a64963` — CI green

Pre-release reconciliation commit: `b64e381` — CI green

Production promotion reference: `staging @ b64e381` fast-forwarded onto `main` (`ad85aa0` → `b64e381`, clean fast-forward, 25 files, no conflicts)

Production workflow: GitHub Actions run `34877682489` on `main`, triggered by `b64e381` — completed, success

## Production verification summary

Independently verified against `amulbham.com` directly (not inferred from workflow success), cache-busted:

- **Human Reader Context**: `FromTheAuthor` present on Invariants/Three SOS, absent on FAFSA/Frontier. Visible `why`/`for` text on Invariants confirmed byte-identical to authored source frontmatter.
- **Semantic Reader Context**: `audience` present as the exact `{ "@type": "Audience", "audienceType": "<verbatim>" }` two-key object on Invariants/Three SOS, matching source; absent on FAFSA/Frontier. `backstory` absent on all four.
- **Reading-shell smoke test** (Invariants, 390px + 1280px): `scrollWidth === innerWidth` exactly at both widths; `.from-the-author` fully contained within `.prose`; mobile TOC `display: block`/desktop rail `display: none` at 390px, correctly inverted at 1280px.
- **PDF production**: all three paper routes (`/research/cybernetics/the-invariants-of-self-organizing-systems/paper.pdf`, `/research/cybernetics/three-self-organizing-systems/paper.pdf`, `/research/policy-systems/an-engineers-guide-to-structural-financial-aid-exploits/paper.pdf`) return `200`. Invariants' production PDF artifact directly downloaded and parsed with `pdfjs-dist`: contains "Abstract," does not contain "From the Author."
- **Regression smoke**: on Invariants and Three SOS, canonical `<link>` === `Article.url` === `mainEntityOfPage.@id`; `author.@id === publisher.@id` (`https://amulbham.com/about/#person`); Invariants' `identifier.value` = `10.5281/zenodo.20531589`; Three SOS's `isBasedOn` = `["https://amulbham.com/research/cybernetics/the-invariants-of-self-organizing-systems/"]`.
- **Indexing**: `robots.txt` still `Disallow: /`; `<meta name="robots" content="noindex, nofollow">` present on every page checked.

No production defect found. No rollback invoked.

## Files changed through Commit C

| File | Commit | Change |
|---|---|---|
| `planning/tickets/T12.4-production-promotion-sprint-12-closure.md` | seal (`7a64963`) | New — ticket contract, `READY` |
| `CHANGELOG.md` | reconciliation (`b64e381`) | Duplicate `T12.2` heading repaired, plus two more of the same pattern (`Sprint 12.1`, `Sprint 12.0`) found by the instructed scan |
| `PLANNING.md` | reconciliation (`b64e381`) | §7 proportionality/scope-economy rule; §11 `DEFECT`-does-not-imply-new-ticket clarification; §13 second maxim |
| `planning/tickets/T12.4-...md` | closure (this commit) | `Status: CLOSED`; this completion report appended |
| `planning/sprints/sprint-12-reader-context.md` | closure (this commit) | Sprint 12 marked `CLOSED`, all six tickets recorded, exit state marked achieved |
| `ROADMAP.md` | closure (this commit) | Sprint 12 "In progress" entry replaced with a completion pointer; Sprint 13/`T13.0` recorded as next |
| `CHANGELOG.md` | closure (this commit) | This `T12.4` entry appended |

No Reader Context runtime file changed in either the reconciliation or closure commits — all product/validator behavior was already shipped and production-verified via Promotion 1.

## Acceptance criteria established through production verification

| # | Criterion | Result |
|---|---|---|
| 1 | Prospectively sealed before release work | PASS — `7a64963` |
| 2 | Duplicate `T12.2` heading reduced to one | PASS |
| 3 | `PLANNING.md` §7 proportionality rule present | PASS |
| 4 | §7 references §5 for evidence depth | PASS |
| 5 | §11 states `DEFECT` ≠ automatic new ticket | PASS |
| 6 | §13 proportional-governance maxim present | PASS |
| 7 | No template/broader redesign | PASS — `planning/templates/`, `planning/programs/`, lifecycle/risk/work-type definitions all untouched |
| 8 | Pre-promotion build/PDF validation green | PASS |
| 9 | `astro check` no new diagnostic | PASS — 3/0/9, 62 files, identical to `T12.3` baseline |
| 10 | Production baseline confirmed before merge | PASS — `main` at `ad85aa0` exactly as expected |
| 11 | Verified staging promoted to `main` | PASS |
| 12 | Production Actions deployment succeeds | PASS — run `34877682489` |
| 13 | Production human matrix correct | PASS |
| 14 | Production semantic matrix correct | PASS |
| 15 | Production phone+desktop smoke clean | PASS |
| 16 | Production PDFs return successfully, Reader Context excluded | PASS |
| 17 | Canonical identity/DOI/relation semantics intact | PASS |
| 18 | Production `noindex, nofollow` + `robots.txt: Disallow: /` | PASS |
| 19 | No production-only regression found | PASS |
| 20–27 | Closure-marking, ROADMAP/CHANGELOG update, drift scan, closure-doc promotion, final workflow, no post-release runtime diff, `T13.0` as sole next gate | Addressed by this commit and the remaining closure steps below — final confirmation is external, post-Promotion-2 evidence, not contained in this commit |

## Documentation corrections

See "Files changed" above — CHANGELOG duplicate-heading repair (3 instances, not the 1 the ticket named, per its own instructed scan) and the Sprint-12-closure ROADMAP/sprint-contract updates.

## Planning-system calibration

`PLANNING.md` §7/§11/§13 — see CHANGELOG entry for full text. No template, risk-class, ticket-state, or work-type redesign.

## Regression confirmation

Full Sprint 8–11 regression surface (canonical identity, Person/publisher, DOI/Highwire, relation projection, PDFs, indexing) reconfirmed unchanged on production by direct observation — see Production verification summary above.

## Deviations from ticket

None in substance. One quantitative deviation explicitly authorized by the ticket's own text: §2A asked to repair "exactly one" duplicate heading but also explicitly instructed "run a narrow scan... for another immediately duplicated ticket heading" — the scan found two more of the identical pattern, both within the Sprint 12 entry range the ticket itself pointed at. Fixed all three under the same low-risk clerical-repair authorization, not as scope expansion.

## Newly discovered findings

The two additional duplicate CHANGELOG headings (Sprint 12.1, Sprint 12.0) — see Deviations above. No other findings.

## Remaining closure steps

```text
Promotion 2 — promote this closure commit to main
Final production workflow — must go green
```

After Promotion 2, the external executor handoff (not this commit) reports: Commit C's resolved hash (`git log -1 --format=%H -- planning/tickets/T12.4-production-promotion-sprint-12-closure.md`), the final closure-documentation promotion reference, the final production workflow result, confirmation that no runtime/product diff occurred after Promotion 1, and the final Sprint 12 closure state. No additional commit is created merely to backfill those values.
