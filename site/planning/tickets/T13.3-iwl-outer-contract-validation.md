# T13.3 — IWL Outer-Contract Validation

Status: CLOSED
Mode: VALIDATION
Risk: R3
Branch: staging
Depends on: T13.2 CLOSED at `cdb1b10`
Unlocks: the Sprint 13 closure decision (close now, or one bounded T13.4)
Inherits: `planning/tickets/T13.1-iwl-outer-contract-decision.md` @ `e723f23` (the frozen outer contract — D1–D8), `planning/tickets/T13.2-iwl-outer-envelope-implementation.md` @ `cdb1b10` (the shipped implementation and its already-run probes/checks), `PLANNING.md` §13 @ `c0e345b` (delta-only/length-cap/commit-choreography discipline this ticket itself follows)

## Goal

Prove or disprove — adversarially, not just via a happy-path build — that the shipped T13.2 implementation actually satisfies T13.1's frozen contract, under real headless-browser measurement and direct deployed-staging inspection. T13.2's own contract explicitly deferred this: "T13.3 will perform the deeper adversarial matrix; T13.2 only needs to prove implementation mechanics."

## Why now

T13.2 closed with real evidence, but its checks were fast and single-viewport: one real Article/IWL pair, local `dist/` inspection plus a light staging spot-check, no responsive/accessibility matrix, no unpublished-state proof against the real committed corpus (T13.2's probes were synthetic and fully reverted before commit).

## Starting state

One real published pair live (`the-chisel-fallacy-what-pangram-actually-measures` + its `/iwl/`), four pre-existing entries with no IWL (true-absence regression surface), zero unpublished IWL entries currently in the committed corpus.

## Decisions already frozen

All of T13.1's D1–D8 and everything T13.2 shipped. This ticket tests them; it does not reopen them — a finding that would require reopening one is a stop condition, not a fix.

## Questions this ticket may answer

None that are new product questions — only whether the existing frozen answers hold under adversarial/responsive/accessibility/regression pressure.

## Scope

- Real headless-Chrome viewport measurement (`puppeteer-core`, the established project method) of both new pages at 375/390/430/768/1024/1280px: containment, no horizontal overflow, breadcrumb/TOC/table behavior.
- Structural accessibility check on `IwlLayout` specifically (new page shape): heading order, landmark roles, breadcrumb `<nav>` semantics, table headers.
- One temporary local probe reintroducing an unpublished IWL for an existing parent, observed then fully reverted (`git status` proven clean) — the real committed corpus has none today, so this is the only way to observe that state against real deployed behavior rather than a synthetic-only proof.
- Direct deployed-staging inspection (not just local `dist/`): zero `application/ld+json` on the IWL page, IWL PDF path `404`, sitemap inclusion, `noindex, nofollow` on both pages, Pagefind fragment for the IWL page resolves and contains real content.
- Full regression matrix on all 4 pre-existing entries: canonical identity, DOI, Highwire tags, relation projections, Reader Context/`audience`, existing PDFs — byte-for-byte unchanged.
- Citation-link spot-check on the real Chisel Article beyond the 3 samples T13.2 already confirmed (at least 3 additional `(Author, Year)` resolutions).

## Explicit exclusions

No inner IWL grammar, no second real IWL fixture, no T13.1 decision reopened, no `main` promotion (T13.4's question, if earned).

## Implementation contract

Not applicable — no product behavior is authored. A defect found here is fixed only if required to meet this ticket's own acceptance criteria (`PLANNING.md` §7); anything larger returns to planning authority as a stop condition, not a silent fix.

## Acceptance criteria

1. Both new pages contain zero horizontal overflow at all 6 measured viewports.
2. `IwlLayout`'s heading order is strictly incremental (no skipped level), breadcrumb uses `<nav aria-label>`, tables have real `<th>` headers.
3. Unpublished-IWL probe: build succeeds, no public route generated, no Article link rendered, probe fully reverted (`git status` empty after).
4. Deployed IWL page: zero `<script type="application/ld+json">`.
5. Deployed IWL PDF path returns `404`.
6. Deployed sitemap includes both new URLs; both pages emit `noindex, nofollow`.
7. Pagefind fragment for the real IWL page resolves with real indexed content (not empty).
8. All 4 pre-existing entries show zero change in canonical identity, DOI, Highwire, relation projections, Reader Context/`audience` output, or PDF content/availability.
9. At least 3 additional citation-link resolutions confirmed correct beyond T13.2's original 3.
10. Zero new `astro check` diagnostics relative to the T13.2 baseline (3 errors / 0 warnings / 9 hints).

## Validation matrix

| Layer | Command/check | Expected result |
|---|---|---|
| Local | `npm run build` / `build:pdfs` / `validate:pdfs` / `astro check` | Unchanged from the T13.2 baseline |
| Local | headless-Chrome matrix, both new pages, 6 viewports | AC1–2 |
| Local | unpublished-IWL probe, create → build → observe → revert | AC3 |
| Staging | direct fetch: both routes, IWL PDF path, sitemap, robots meta | AC4–6 |
| Staging | decompressed Pagefind fragment for the IWL URL | AC7 |
| Staging | regression fetch, all 4 pre-existing entries + homepage + `ai-systems` pillar | AC8 |

## Adversarial tests

| Mutation | Expected failure/behavior | Restoration proof |
|---|---|---|
| Unpublished IWL added for an existing parent (e.g. Invariants) | No route generated, no Article-side link, build succeeds | `git status --short` empty after delete |

Duplicate-parent and invalid-parent fail-closed behavior were already proven at T13.2 (Probes D/E) — not re-run here; citing that evidence rather than reproducing it (`PLANNING.md` §13 evidence economy).

## Regression boundaries

Every Sprint 7–12 invariant, plus T13.2's own baseline: 43 pages, 5 research objects, identical `astro check` counts.

## Documentation updates

None, unless a real defect is found and fixed — in which case only the affected canonical surface, named in the completion report.

## Git and deployment boundary

Seal commit, then — since VALIDATION mode with no product behavior authored normally produces no separate implementation diff — one completion/closure commit (matching T12.3's own precedent and this calibration's R0/R1 collapse pattern; VALIDATION risk ceiling reflects what's being validated, not commit count). If a real defect requires a runtime fix to meet acceptance criteria, that fix lands in this same closure commit, not a separate one. No `main` promotion.

## Stop conditions

Any finding that would require reopening a T13.1 decision, or any defect whose fix would require a new architecture/product decision — stop, return to planning authority.

## Required completion report

Standard template, R3 cap (100 lines).

## Done when

All 10 acceptance criteria pass with direct evidence (not narration), the probe is proven reverted, regression is confirmed on all 4 pre-existing entries, and the completion report states plainly whether Sprint 13 can close from this evidence or whether one bounded `T13.4` is genuinely earned — without pre-drafting that ticket.

---

# T13.3 Completion Report

Result: COMPLETE
Branch: staging
Commit: seal `60d2e58`; this closure commit resolved via `git log -1 --format=%H -- planning/tickets/T13.3-iwl-outer-contract-validation.md`
Deployment: staging only, no `main` promotion — validation found nothing requiring a runtime fix

## Outcome

All 10 acceptance criteria pass with direct evidence. Zero blocking defects. Three genuine, precisely-scoped findings surfaced, all pre-existing/sitewide, none IWL-specific, none fixed here.

## Acceptance criteria

| # | Result | Evidence |
|---|---|---|
| 1 | PASS | Headless-Chrome, 6 viewports × 2 pages: `overflow=no` on every cell |
| 2 | PASS (assumption corrected) | Heading order non-skipped on both pages; 3/3 and 14/14 tables have real `<th>`. **Correction**: breadcrumbs render as `<p class="breadcrumbs meta">`, not `<nav aria-label>` — a wrong assumption in this ticket's own AC wording, not a defect: `Breadcrumbs.astro` is a shared component, IwlLayout inherited the identical, pre-existing sitewide shape correctly |
| 3 | PASS | Unpublished probe on `three-self-organizing-systems`: build succeeded, no route dir, no Article link, `git status` empty after delete |
| 4 | PASS | `grep -c application/ld+json` on deployed IWL page = `0` |
| 5 | PASS | Deployed `.../iwl/paper.pdf` → `404` |
| 6 | PASS | Both URLs in `sitemap-0.xml`; `noindex, nofollow` on both deployed pages |
| 7 | PASS | Fresh local Pagefind fragment for the IWL URL decompressed, real content confirmed (`"url":".../iwl/"`) |
| 8 | PASS | `validate-research-semantics`: 5 objects, 0 issues; direct byte-check of `isBasedOn`, both `audience` projections, DOI, and correct absence on FAFSA/Frontier all verbatim-unchanged; `validate:pdfs` 4/4 |
| 9 | PASS | 5 of 6 additional citations resolved correctly (Tao→27, Reinhart et al.→25, Rodrigues→26, Goodhart→16, Queloz & Beckmann→24) |
| 10 | PASS | `astro check`: 3 errors / 0 warnings / 9 hints, 65 files — identical to T13.2 baseline |

## Adversarial tests

| Mutation | Expected | Observed | Restored |
|---|---|---|---|
| Unpublished IWL for existing parent | No route/link | Confirmed | Yes, `git status` clean |

## Newly discovered findings (unrelated, not fixed)

1. **Header brand link is an `<h2>` appearing before the page `<h1>`** — `src/components/Header.astro`, sitewide, present on every page including all 4 pre-existing entries; predates Sprint 13 entirely.
2. **`FormattedDate.astro` uses `.toLocaleDateString()` without `timeZone: 'UTC'`** — a locally-built page in a timezone behind UTC can show the previous calendar day (confirmed: local build showed "Sep 21," deployed staging, built by CI in UTC, correctly shows "Sep 22"). Sitewide, pre-existing, no confirmed production impact.
3. **`rehype-citation-links.mjs` doesn't link a mixed personal+organizational two-author citation** — `(Ahlstrom & Dragonsteel Books, 2025)` stays plain text despite `ref-2` existing; the linker's two-author `&`-key registration expects two personal-name entries. Sitewide pipeline, pre-existing, not IWL-specific.

## Regression confirmation

All 4 pre-existing entries: canonical identity, DOI, Highwire, relation projection, Reader Context/`audience`, and PDF availability confirmed byte-identical. `SITE_WIDE_NOINDEX` unchanged. Page/word counts back to the pre-probe baseline after every temporary mutation.

## Documentation reconciliation

None required — no defect met the bar for a fix.

## Remaining gate

Sprint 13's own exit state (`sprint-13-iwl-foundation.md`) requires production promotion, matching Sprint 12's `T12.4` precedent. This evidence finds nothing blocking it: **`T13.4` (RELEASE — production promotion + Sprint 13 closure freeze) is earned**, not merely possible. Not drafted here, per this ticket's own instruction.
