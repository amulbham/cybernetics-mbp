# T15.1 — Accept OPAQUE V1 and Close Sprint 15

Status: READY
Mode: RELEASE
Risk: R3 because this closes the sprint and promotes the documentation lineage to `main`. Evidence is scaled to that docs-only risk: clean ancestry, a documentation path list, a fast-forward, and a green Actions run. No product/runtime HTTP QA.
Branch: `staging`, then fast-forward to `main`
Depends on: T15.0 CLOSED at `92862f2`; Amul accepted OPAQUE V1 on 2026-09-25. `origin/main` is `6784e4d`, an ancestor of `staging`.
Unlocks: the Research Object MVP as direction only. Not a next sprint and not a Sprint 16–19 ticket.
Inherits: `planning/sprints/sprint-15-minimum-iwl-formalization.md` @ `92862f2`; `planning/tickets/T15.0-iwl-formalization-necessity-audit.md` @ `92862f2`

## Goal

Freeze OPAQUE V1 as the Sprint 15 result, close the sprint's canonical records, and fast-forward that documentation to `main`.

## Why now

T15.0 recommended OPAQUE V1. Amul accepted it for the currently accepted Research Object MVP. The sprint contract still says EXECUTING, and `PUBLISHING.md` still says inner vocabulary is deferred to Sprint 15+.

## Starting state

Pre-seal review, 2026-09-25. No BLOCKER. `staging` @ `92862f2` is ahead of `origin/main` @ `6784e4d` by the T15.0 seal and closure only. Private matrix H and outputs A–G remain outside Git. The public IWL is unchanged.

REQUIRED before seal, applied in this ticket's text: `AGENTS.md` carries the same present-tense "deferred to Sprint 15+" sentence as `PUBLISHING.md`. One matching sentence is in scope so the two canonical present-tense statements do not disagree. That is not a new IWL specification.

OPTIONAL prose was skipped.

## Decisions already frozen

Amul accepted OPAQUE V1. The outer envelope stays authoritative. The inner body stays prose-first authored Markdown. No inner machine schema was earned for the current MVP. The two Chisel editorial mismatches stay recorded and are not fixed here. Closed Sprint 14 records and closed T15.0 are not edited.

## Questions this ticket may answer

- Did the docs-only lineage fast-forward cleanly?
- Did the Actions run for that `main` SHA succeed?

## Scope

- Record the frozen v1 position and this revisit trigger: reopen inner formalization only when a later accepted operation demonstrably cannot be performed, or becomes unsafe or meaning-losing, after the existing outer contract and an appropriate editorial convention have been tried.
- Close the Sprint 15 contract from T15.0's public report and private matrix H, without reopening H or A–G and without new evidence.
- Reconcile the program arc: Sprint 15 CLOSED on OPAQUE V1; Sprint 16 optional and not authorized, and only before launch if a genuinely different second intellectual history exists and product authority chooses it as a generalization test; collapse the old Sprint 16 ticket queue while keeping a historical trace; keep the Sprint 17 Keystatic label, mark that sequence deferred post-MVP and unscheduled, and collapse queue language that makes Keystatic the next pre-launch step. Do not renumber Sprint 17.
- Add the directional Research Object MVP block to `ROADMAP.md`. It is not execution authority and does not claim the current corpus is Production Ready. Keep the two editorial findings on the later content pass.
- Replace the stale Sprint 15+ deferral in `PUBLISHING.md` §2a item 8, and the same sentence in `AGENTS.md`, with the OPAQUE V1 position.
- Add the `CHANGELOG.md` entry.
- Fast-forward to `main` and confirm Actions. If this closure commit cannot cite its own run, one later docs-only evidence commit may record the run that already finished. Do not add a commit whose only job is to cite itself.

## Explicit exclusions

No runtime, content-schema, public Article/IWL, route, PDF, indexing, robots, sitemap, structured-data, or validator change. No new evidence. No edit to closed tickets or the closed Sprint 14 contract. No Sprint 16–19 contract or ticket. No next-sprint draft. No manual deploy. No product HTTP QA unless CI or the diff shows a reason.

## Implementation contract

- OPAQUE V1 means the outer contract plus authored Markdown. It does not mean the current five works are Production Ready.
- Sprint 16 is not a queue. Sprint 17 is not the next pre-launch step.
- The MVP block is direction. It has no acceptance criteria and no ticket contracts.
- Private files stay outside Git.

## Acceptance criteria

1. Sprint 15 reads CLOSED on OPAQUE V1, with the revisit trigger stated.
2. `PUBLISHING.md` and the matching `AGENTS.md` sentence no longer defer inner vocabulary to Sprint 15+.
3. The program arc and `ROADMAP.md` match the reconciliation above, including the directional MVP block and the two deferred editorial findings.
4. The changed-file list is documentation only. `main` fast-forwards with no merge commit and no unrelated commits.
5. The Actions run for the promoted closure SHA is green, and any single evidence-note commit's run is green too.

## Validation matrix

| Layer | Check | Expected result |
|---|---|---|
| Ancestry | `origin/main` is an ancestor of the closure SHA | Fast-forward only |
| Diff | `git diff --name-only` | Documentation paths only |
| Position | Read `PUBLISHING.md` §2a item 8 | OPAQUE V1, no new spec |
| CI | Actions for the `main` SHA | Success. A failure stops the release claim |

No local build. No HTTP QA.

## Adversarial tests

- Read the MVP block as a claim that five works are already Production Ready. Expected: the block says the target is five genuinely ready works and does not say the current corpus has passed.
- Read Sprint 16 or Sprint 17 as the next authorized sprint. Expected: both are explicitly not authorized.
- Restoration: no public content file in the diff.

## Regression boundaries

Public IWL, Article, schemas, validators, workflows, routes, and indexing stay unchanged. T15.0's ticket body stays unchanged. H and A–G stay unedited and untracked.

## Documentation updates

This ticket, the Sprint 15 contract, `planning/programs/research-object-arc-13-17.md`, `ROADMAP.md`, `CHANGELOG.md`, `PUBLISHING.md` §2a item 8, and the one matching `AGENTS.md` sentence.

## Git and deployment boundary

Seal commit, then one closure commit, on `staging`. Fast-forward `origin/main` from `6784e4d`. No merge commit. No force-push. Pushing `main` starts Deploy Pages. Confirm that run green before calling the release accepted. One evidence commit is allowed after that run, then confirm the evidence commit's run, then stop.

## Stop conditions

Stop if ancestry is not clean, the diff leaves the authorized documentation files, a reconciliation changes product meaning beyond OPAQUE V1, CI fails, or accepting OPAQUE V1 would require reopening a material Sprint 15 decision.

## Required completion report

Use `planning/templates/completion-report.md`. R3 cap: 100 lines. Name SHAs, the path list, the Actions run once known, and the next planning gate. Do not draft that gate's sprint.

## Done when

Sprint 15 is CLOSED on OPAQUE V1, `main` is the fast-forward of this lineage, and the promotion's Actions run is green.
